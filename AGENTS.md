## Agent skills

### Issue tracker

Issue 与 spec 以 markdown 文件形式存放在本仓库 `.scratch/<feature>/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

沿用五个标准 triage 角色，标签字符串与角色名同名。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）：仓库根目录一个 `CONTEXT.md`，加上 `docs/adr/`。详见 `docs/agents/domain.md`。
