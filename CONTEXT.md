# Avalonia-scanner

一个用 Avalonia 作为跨平台 UI、并承载 MAUI 原生相机预览的 Android 条码扫描应用。本文件只记录本项目特有的术语，不记录通用编程概念。

## Language

### 扫描界面

**相机预览（camera preview）**：
扫描页中实时渲染的相机画面。它是活的取景画面，不是拍照或截图后的静态图像。
_Avoid_: 快照、静态预览、拍到的图、截图预览

**悬浮按钮（overlay buttons）**：
叠加在相机预览之上、与相机同处一个原生视图树的控件。它们能显示在画面之上，靠的正是这个位置关系，而非 UI 框架的层叠顺序。
_Avoid_: 浮动按钮、漂浮按钮、覆盖层按钮、浮层控件

**命令栏（command bar）**：
扫描页底部独立一行的三个按钮（Torch / Cancel / Camera），位于相机预览区域之外，与悬浮按钮是同一组操作的第二个入口。
_Avoid_: 工具栏、按钮栏、底栏、操作栏

**扫描页（scanner page）**：
进入扫描后才出现、覆盖在主页之上的整页取景界面，包含相机预览与命令栏。
_Avoid_: 扫描窗口、相机页、取景页
